#include "main.h"

/**
 * _isupper - check if c is uppercase
 *
 * return - (1 if uppercase; 0 if else)
 *
 */

int _isupper (int c)
{
	if (c = ['A'-'Z'])
	{
		return (1);
	}
	if else
	{ 
		return (0);
	}
}
